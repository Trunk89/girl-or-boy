# Girl or Boy? - Gender reaveal game

## Usage

GENDER should be set to `GIRL` or `BOY` to match the enum.

BABY_NAME is a string.

### Example:

```bash
export BABY_NAME=Anna
```

```bash
export GENDER=GIRL
```
